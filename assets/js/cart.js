function getCart(){return JSON.parse(localStorage.getItem('cart')||'[]')}function saveCart(c){localStorage.setItem('cart',JSON.stringify(c));updateCartCount()}function addToCart(p){const c=getCart();const f=c.find(x=>x.id===p.id);if(f)f.qty++;else c.push({...p,qty:1});saveCart(c);alert('Produit ajouté au panier')}function updateCartCount(){const el=document.getElementById('cart-count');if(el)el.textContent=getCart().reduce((s,i)=>s+i.qty,0)}updateCartCount();
