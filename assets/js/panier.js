function renderCart(){const c=getCart();const total=c.reduce((s,i)=>s+i.price*i.qty,0);document.getElementById('cart-view').innerHTML=c.length?c.map(i=>`<div class="card"><b>${i.title}</b><p>${i.qty} x ${i.price} €</p></div>`).join('')+`<h2>Total : ${total.toFixed(2)} €</h2>`:'Panier vide'}document.getElementById('checkout').onclick=async()=>{const items=getCart();const r=await fetch('/api/checkout/create-session',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({items})});const d=await r.json();if(d.url) location.href=d.url; else alert(d.error||'Stripe non configuré')};renderCart();
