let products=[];async function load(){products=await fetch('/api/products').then(r=>r.json());render(products)}function render(list){document.getElementById('products').innerHTML=list.map(p=>`<article class="product"><img src="${p.image||'https://picsum.photos/seed/x/700/600'}"><h3>${p.title}</h3><p>${p.description||''}</p><div class="price">${Number(p.price).toFixed(2)} €</div><button class="btn" onclick='addToCart(${JSON.stringify({id:p.id,title:p.title,price:p.price,image:p.image}).replaceAll("'","&apos;")})'>Ajouter au panier</button></article>`).join('')}document.getElementById('search').addEventListener('input',e=>{const q=e.target.value.toLowerCase();render(products.filter(p=>(p.title+p.description).toLowerCase().includes(q)))});load();
